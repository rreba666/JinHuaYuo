# 2026-09-07 商品详情页新增销量展示

## 改动（`mini_shop/subpkg-goods/detail/detail.vue`）
- 商品信息区价格下方新增销量展示：`已售 {{ product?.soldCount }}`（`ProductDetail.soldCount`），用 `v-if` 控制（0 或无值时不显示）。
- 新增样式 `.sold-count`（灰色小字，位于价格下方）。

## 说明
- 数据来自详情接口 `ProductDetail.soldCount`（已存在），前端仅展示，无需新增接口。
- 需 HBuilderX 重新编译，真机/微信预览验证商品详情价格下方出现"已售 xx"。

## 提交
- `git add` 后提交 `mini_shop/subpkg-goods/detail/detail.vue`。
