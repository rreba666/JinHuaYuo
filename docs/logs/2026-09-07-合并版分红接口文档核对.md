# 2026-09-07 合并版分红接口文档最终核对

## 依据
`docs/plan/wxstore分红-前端接口文档(合并版).md`（最终权威版，v10「大池子」+ 方案C动态槽位）。旧文档已停用。

## 已完成/已对齐（B 端）
- 手动结算、手动发放按钮：已移除（只读提示）。
- **重置购买机会按钮**：本轮已移除——接口 `PUT /api/admin/profit/limit/{userId}/reset` 已删除（404），同步清理视图按钮、`store.resetLimit`、`api.resetDividendLimit`。
- **分红随机浮动幅度配置**：本轮修正接口路径为**通用配置接口** `/api/admin/setting/dividend_random_float`（下划线），POST body `{configValue, remark}`（此前误用横杠 `dividend-random-float` 专用路径，已改正）。
- 每日红包未结算/已结算两卡片：已实现（用户确认当前正常）。

## 待确认 / 待开发项
1. **C 端槽位展示（方案C，清单#9 必做）**：`/api/wallet/dividend-slots` 目前**无前端展示页面**（仅 `api/user.ts` 定义了 `getDividendSlots`，无页面调用）；且 `DividendSlot` 类型**缺** `invalidFlag`/`invalidReason` 字段。合并版要求槽位列表区分 `locked=1`（满额锁死）/ `invalidFlag=1`（退款作废）。
2. **`dividend_slot_count` 配置**（默认3，清单#4 可选）：槽位数量上限配置入口，通用接口 `/api/admin/setting/dividend_slot_count`，前端未做。
3. **每日红包 `settled_flag` 口径**：合并版改为 `settled_flag` 驱动 + 字段 `settledFlag/settledAmount/cumulativeUserCount`；前端当前按旧 `pool_id` 口径实现（显示 poolId/dailyUserCount）。用户此前确认"此功能正常"，暂未改。
4. **秒退窗口**：合并版第十四节为 wxstore 口径（物流7天/自提30天）；当前项目为**今华有肽（30分钟）**，`detail.vue` 实现正确，请勿按合并版改。

## 提交
- prod `ac7cfc3` fix(admin): 修正分红浮动配置接口为通用配置路径(dividend_random_float), 移除已删除的重置购买机会按钮。
